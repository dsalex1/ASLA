rules_version = '2';

// Production rules: the whole database belongs to the signed-in band, so any
// authenticated user may read and write it, and nobody else may touch it.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
